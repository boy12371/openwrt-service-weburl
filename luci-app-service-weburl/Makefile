include $(TOPDIR)/rules.mk

LUCI_TITLE:=LuCI Support for Service WebURL
LUCI_DEPENDS:=+luci-base +luci-compat +sqlite3
LUCI_PKGARCH:=all

PKG_VERSION:=1.0.0
PKG_RELEASE:=1

include $(TOPDIR)/feeds/luci/luci.mk

# call BuildPackage - OpenWrt buildroot signature